#include <stdio.h>

int main(){
    // 3. Twenty integers are to be stored in memory. What will you prefer- Array or  structure? 
    // Answer:The answer is array because the array is shortest way to store 20 integers at one time.
    return 0;
}