#include <stdio.h>

int main(){
    int a=6;
    int b=3;
    int c=a+b;
    printf("The value of a is %d and the value of b is %d and sum is %d\n",a,b,c);
    //The modulus operator is used to get the remainder
    printf("The remainder when a is divided with b is %d\n",a%b);

    //This does not work exponentitation in c
    //int d=a^b
      
    
    
    return 0;
}