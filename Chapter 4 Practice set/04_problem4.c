#include <stdio.h>

int main(){
    /*  What can be done using one type of loop can also be done using the other two 
types of loops – true or false? */
     //True.
     //Yes the answer is true because because a work can be done using one type of loop can also be done using the other two types of loops.
    return 0;
}